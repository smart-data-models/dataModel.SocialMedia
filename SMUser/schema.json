{
  "$schema": "http://json-schema.org/schema#",
  "schemaVersion": "0.0",
  "$id": "https://raw.githubusercontent.com/smart-data-models/dataModel.SocialMedia/master/SMUser/schema.json",
  "title": "Social Media - SMUser",
  "description": "This entity contains a harmonised description of a generic SMUser made for the Social Media domain. This entity is primarily associated with the description of a user of Social Media applications.",
  "type": "object",
  "allOf": [
    {
      "$ref": "https://smart-data-models.github.io/data-models/common-schema.json#/definitions/Location-Commons"
    },
    {
      "properties": {
        "type": {
          "type": "string",
          "description": "Property. NGSI-LD Entity Type. It must be equal to SMUser.",
          "enum": [
            "SMUser"
          ]
        },
        "userId": {
          "type": "string",
          "description": "Property.  Model:'https://schema.org/Text'. Units:'No unit'. The User ID of the SMUser."
        },
        "platform": {
          "type": "string",
          "description": "Property. Description of the  social platform of the user."
        },
        "userName": {
          "type": "string",
          "description": "Property.  Model:'https://schema.org/Text'. Units:'No unit'. The username of the SMUser."
        },
        "createdPosts": {
          "type": "array",
          "description": "Relationship. The IDs of the post that the SMUser created.",
          "items": {
            "type": "string",
            "format": "uri"
          }
        },
        "isMentionedBy": {
          "type": "array",
          "description": "Relationship. The IDs of the posts that mention the SMUser.",
          "items": {
            "type": "string",
            "format": "uri"
          }
        }
      }
    }
  ],
  "required": [
    "id",
    "type",
    "platform",
    "userName"
  ]
}
